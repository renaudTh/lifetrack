# Development environment (devcontainer). Copy to .env.local.
# Must match what the `db` service declares in .devcontainer/docker-compose.yaml.

DB_HOST=db
DB_PORT=5432
DB_NAME=lifetrack-dev
DB_USERNAME=lifetrack
DB_PASSWORD=devpass

AUTH0_AUDIENCE=lifetrack-api
# Trailing slash required, see jwt.strategy.ts
AUTH0_TENANT=https://dev-80v0aoz3uxk711xg.us.auth0.com/
